api: true
rtspTransports: [udp, tcp]

authInternalUsers:
  - user: any
    pass:
    ips: []
    permissions:
      - action: publish
        path: neo
      - action: read
        path: neo
      - action: playback
        path: neo

  - user: admin
    pass: change-me-admin
    ips: []
    permissions:
      - action: api
      - action: metrics
      - action: pprof

paths:
  all_others:
